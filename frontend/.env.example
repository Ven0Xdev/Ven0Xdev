# Must include the /api/v1 prefix — every call in lib/api.ts appends a
# path like /stocks/{symbol}/analysis directly onto this base, with no
# prefix of its own. Setting this to just http://localhost:8000 (without
# /api/v1) will 404 every API call.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
